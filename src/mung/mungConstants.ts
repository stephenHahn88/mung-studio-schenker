/**
 * Maximum size of the mask bitmap.
 *
 * This is not a limitation of MuNG as such, this is a reasonable limit
 * to prevent unexpected OOM crashes of the app.
 */
export const MUNG_MAX_MASK_SIZE = 4096 * 3;
