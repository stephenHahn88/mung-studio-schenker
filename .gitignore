# project
node_modules
.parcel-cache
dist
# build-output backup/staging copies from atomic deploy swaps (never commit)
dist_new
dist_old
dist.*
.mypy_cache
logs
__pycache__
simple-php-backend/__pycache__

# editors
.idea
# .vscode is not ignored - it has proper settings for tabsize

# operating system
.DS_Store

# dotenv
.env
.env.production

# local release data
models/**
!models/
!models/README.md
documents/*
!documents/.gitkeep
!documents/README.md
# one-off local data backups (kept on disk, never committed)
documents_backup_*

# secrets (real per-annotator tokens — never commit; use users.example.json as template)
simple-php-backend/users.json
