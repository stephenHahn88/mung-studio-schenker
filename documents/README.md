Local annotation documents live here.

Each document should be a directory with this shape:

```text
documents/page_001/
  image.png
  mung.xml
```

The image file may be named `image.png`, `image.jpg`, or `image.jpeg`.
